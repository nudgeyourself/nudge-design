import React from "react";
import { render } from "@testing-library/react";

import HeaderAction from "./HeaderAction";

describe("HeaderAction", () => {
  test("renders the HeaderAction component", () => {
    render(<HeaderAction />);
  });
});